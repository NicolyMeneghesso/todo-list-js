{
  "name": "todo-list",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch db.json --host 0.0.0.0 --port 3000 --static ./",
    "dev": "concurrently \"json-server --watch db.json --host 0.0.0.0 --port 3000 --static ./\" \"live-server --port=8080 --host=0.0.0.0 --watch=js,css,html --delay=1000\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "json-server": "^0.17.4",
    "live-server": "^1.2.2",
    "concurrently": "^8.2.2"
  }
}
